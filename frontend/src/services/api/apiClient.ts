export async function apiFetch<T>(path: string, init?: RequestInit): Promise<T> {
  // TODO: conectar a FastAPI endpoint /recipes/recommend /users/login /pantry
  throw new Error('apiFetch todavía no está implementado en el prototipo.');
}
